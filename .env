# shared dev defaults (committed). Personal overrides go to .env.local
# (gitignored). Real environments set real env vars — files never win over
# them.
HTTP_ADDR=:8080
DB_PATH=app.db
LOG_LEVEL=debug
LOG_FORMAT=text
